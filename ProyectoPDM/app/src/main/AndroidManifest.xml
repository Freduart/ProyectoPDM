<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sv.ues.fia.eisi.proyectopdm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NuevaEvaluacionActivity" />
        <activity android:name=".NuevaSolicitudImpresionActivity" />
        <activity android:name=".SolicitudImpresionActivity" />
        <activity android:name=".CicloActivity" />
        <activity android:name=".LocalActivity" />
        <activity android:name=".EvaluacionActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NuevaSolicitudExtraordinarioActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>